(ns async-crawler.core-test
  (:require [clojure.test :refer :all]
            [async-crawler.core :refer :all]))

;(deftest a-test
;  (testing "FIXME, I fail."
;    (is (= 0 1))))

(deftest a-test
  (testing "FIXME, I pass."
    (is (= 1 1))))
